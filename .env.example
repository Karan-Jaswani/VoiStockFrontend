# Copy this to .env.local and fill values. Do NOT commit your actual .env files.
# Vite env vars must be prefixed with VITE_
VITE_VOISTOCK_API_URL=https://your-api.example.com
VITE_LOGO_URL=/VoiStock_Logo.png
# Add other public, non-secret values here
